/**
 * Рабочий план счетов — подмножество Типового плана счетов
 * бухгалтерского учёта РК, используемое проекциями MVP.
 */
export const ACCOUNTS = {
  /** Денежные средства на текущих банковских счетах. */
  CASH_BANK: '1030',
  /** Краткосрочная дебиторская задолженность покупателей и заказчиков. */
  TRADE_RECEIVABLES: '1210',
  /** Товары. */
  INVENTORY_GOODS: '1330',
  /** НДС к возмещению (зачёту). */
  VAT_RECEIVABLE: '1420',
  /** Обязательства по НДС. */
  VAT_PAYABLE: '3130',
  /** Краткосрочная кредиторская задолженность поставщикам и подрядчикам. */
  TRADE_PAYABLES: '3310',
  /** Доход от реализации продукции и оказания услуг. */
  REVENUE: '6010',
} as const;

export type AccountCode = (typeof ACCOUNTS)[keyof typeof ACCOUNTS] | (string & {});
